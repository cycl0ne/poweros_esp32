# Build output
.zig-cache/
zig-out/

# The Espressif Zig and QEMU, fetched by ./zig and scripts/build-qemu.sh
toolchain/

# Local assistant files
CLAUDE.md
.claude/

# Files dropped onto the disk image (see build.zig: disk_tree)
/disk/

# Work lists, kept locally
todo/
